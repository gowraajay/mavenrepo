pipeline{

agent{ any }

stage('SCM') {
  
step([$class: 'GitHubSetCommitStatusBuilder'])
checkout scmGit(branches: [[name: '*/master']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/gowraajay/mavenrepo.git']])
}
stage('Build') {
    
	steps{
	sh '/usr/share/maven/bin/mvn package'
	}
}
 }